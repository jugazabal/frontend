# Example environment variables
# Port for the Express backend (defaults to 3001 if unset)
PORT=3001
# Optional API base path override for frontend (usually leave empty to use same-origin /api)
VITE_API_BASE=
# MongoDB connection string (required). Use a user with least privileges.
# Example Atlas URI:
# MONGODB_URI=mongodb+srv://<user>:<password>@cluster0.a5qfl.mongodb.net/noteApp?retryWrites=true&w=majority&appName=Cluster0
MONGODB_URI=mongodb+srv://<user>:<password>@cluster0.a5qfl.mongodb.net/noteApp?retryWrites=true&w=majority&appName=Cluster0
# Separate test database URI (used when NODE_ENV=test). Should point to a different DB (e.g. testNoteApp)
# TEST_MONGODB_URI=mongodb+srv://<user>:<password>@cluster0.a5qfl.mongodb.net/testNoteApp?retryWrites=true&w=majority&appName=Cluster0
TEST_MONGODB_URI=
# Additional allowed CORS origins (comma separated, besides GitHub Pages origin)
ALLOWED_ORIGINS=
